{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "productos",
            "database": "${workspaceFolder:PLAN_B}/database/productos.db"
        },
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "users",
            "database": "${workspaceFolder:PLAN_B}/database/users.db"
        }
    ]
}